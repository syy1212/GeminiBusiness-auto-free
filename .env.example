# ========================================
# Gemini Enterprise 自动注册工具 - 环境变量配置
# ========================================
# 
# 使用说明：
# 1. 复制此文件并重命名为 .env
# 2. 根据实际情况填写下方配置项
# 3. .env 文件不会被提交到 Git 仓库，请妥善保管
#

# ========================================
# 邮箱域名配置（必填）
# ========================================
# 用于生成随机邮箱地址的域名
# 示例：XXXX.com
DOMAIN=your-domain.com

# ========================================
# 验证码获取方式配置
# ========================================
# 支持两种方式：
# 1. tempmail.plus API（推荐，更快更稳定）
# 2. IMAP/POP3 邮箱收取（适用于自建邮箱服务器）
#
# 选择方式：
# - 如果使用 tempmail.plus，填写下方 TEMP_MAIL 相关配置
# - 如果使用 IMAP/POP3，将 TEMP_MAIL 设置为 null，并填写 IMAP 相关配置

# ----------------------------------------
# 方式1: tempmail.plus API 配置
# ----------------------------------------
# tempmail.plus 用户名（@ 符号前的部分）
# 如果不使用 tempmail.plus，请设置为 null
TEMP_MAIL=your_username

# tempmail.plus 的 epin（API密钥）
TEMP_MAIL_EPIN=123456

# 临时邮箱后缀
# 示例：@mailto.plus
TEMP_MAIL_EXT=@mailto.plus

# ----------------------------------------
# 方式2: IMAP/POP3 邮箱配置
# ----------------------------------------
# 仅当 TEMP_MAIL=null 时生效

# 邮件协议类型：IMAP 或 POP3
# 默认值：POP3
IMAP_PROTOCOL=POP3

# IMAP/POP3 服务器地址
# 示例：imap.gmail.com 或 pop.gmail.com
IMAP_SERVER=imap.your-server.com

# IMAP/POP3 服务器端口
# IMAP 常用端口：993（SSL）、143（非SSL）
# POP3 常用端口：995（SSL）、110（非SSL）
IMAP_PORT=993

# 邮箱账号（完整邮箱地址）
IMAP_USER=your@email.com

# 邮箱密码或授权码
# 注意：Gmail、QQ邮箱等需要使用"应用专用密码"或"授权码"
IMAP_PASS=your_password_or_app_password

# 邮箱收件箱目录名
# IMAP 默认：inbox 或 INBOX
# 部分邮箱可能使用其他名称，如：收件箱
IMAP_DIR=inbox

# ========================================
# 浏览器配置（可选）
# ========================================

# 是否使用无头模式（后台运行，不显示浏览器窗口）
# 值：true 或 false
# 默认：false（显示浏览器窗口，便于调试）
BROWSER_HEADLESS=false

# HTTP 代理服务器地址（可选）
# 如果不需要代理，留空或删除此行
# 格式：http://IP:端口 或 socks5://IP:端口
# 示例：http://127.0.0.1:7890
BROWSER_PROXY=

# 自定义浏览器可执行文件路径（可选）
# 如果不指定，将使用系统默认的 Chrome 或 Chromium
# Windows 示例：C:/Program Files/Google/Chrome/Application/chrome.exe
# Windows Edge 示例：C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe
# macOS Chrome 示例：/Applications/Google Chrome.app/Contents/MacOS/Google Chrome
# Linux 示例：/usr/bin/google-chrome
BROWSER_PATH=

# 自定义 User-Agent（可选）
# 如果不指定，将使用浏览器默认 UA
# 示例：Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
BROWSER_USER_AGENT=
